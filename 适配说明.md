# 适配记录：0.1.2-rc.1 本地适配（dsh-done-whale → dsh-notice-center）

> **状态（2026-09-13）**：本项目已独立为 **dsh-notice-center v1.0.0** —— 包名 ``dsh-notice-center``、插件 id ``notice-center``、设置命名空间 ``notice-center``，采用「冻结式独立」（上游基线见 ``upstream/v0.2.2/``，历史补丁流水线见 ``docs/patches-archive/``）。本文件保留为 0.1.2-rc.1 时期的适配说明与验证清单，文中出现的旧包名与旧版本号（``0.2.2-adapt.1``）均为当时记录；当前项目说明见 ``README.md``。

> 注：`lib/index.mjs`、`lib/client.cjs` 已与准备提交上游的 **PR 版逐字节一致**（分支 `fix/dsh-0.1.2-rc.1`，commit `b772d8b`，已推到 fork `SCP-QQ/dsh-done-whale`）；`package.json` 保留本地分发元数据（版本号 `0.2.2-adapt.1`、依赖/peer 范围），运行时相关字段（`dsh.client.inject`）与 PR 版等价。旧实现已备份在 `lib/*.bak-single-20260912-234344`。

上游：[wally8-8/dsh-done-whale](https://github.com/wally8-8/dsh-done-whale) v0.2.2（MIT）。完整调研见：本地存档《dsh-done-whale-适配性研究报告.md》；PR 正文见 本地存档《PR-描述.md》。

## 相对上游 v0.2.2 的改动

### 1. 宿主半 `lib/index.mjs`（1 处）
`@deepseek-ai/dsh-settings` 自 0.1.2-rc.1 起**不再导出 `settingsNamespace()`**（register 改为内部 `parseSettingsNamespace()` 校验小写连字符字符串），而具名导入在缺少该导出时会直接报错。
→ 改为命名空间导入 `import * as dshSettings`，注册处用 `resolveNamespace()`：有该助手就用（≤0.1.1-rc.2），没有就直接传 `"done-whale"`（0.1.2-rc.1+）。两个版本线都可用。

### 2. 浏览器半 `lib/client.cjs`（琥珀信号迁移）
0.1.2-rc.1 的 `SessionSummary` 行**已无 `pendingInteraction` 字段**；「有待处理交互」改为独立通道：
`@deepseek-ai/dsh-client-ui-session` 提供的 `ctx.uiSession.pendingInteractions`（`{ getSnapshot(): Map<sessionId, interaction>, subscribe(listener) }`，approval/提问处理完自动移除）。
→
- 新增 `let pendingStore`，并用 `ctx.inject(["uiSession"], …)` 建立**可选通道**订阅 `pendingInteractions`（`ctx.inject` 声明的是必需服务，服务缺席时该子 fiber 不加载 → 旧版本自动回退行字段，不回归）；
- `targetOf()` 的琥珀判断改为 `pendingMap?.has(row.id) || row.pendingInteraction !== void 0`（仍主会话 only、绿优先）。

绿灯（`row.completed`）与黑/绿/琥珀颜色配置面板逻辑**未动** —— 0.1.2-rc.1 中 `completed/origin/running/byId/current` 与 `settingsScope`/`settings.section`/`locale` 契约与插件假设一致。

### 3. `package.json`
- version → `0.2.2-adapt.1`；description 注明本地适配版。
- `dsh.client.inject`：移除已停发的 `@deepseek-ai/dsh-client-runtime`，加入
  `@deepseek-ai/dsh-client-locale`、`@deepseek-ai/dsh-client-ui-settings`、`@deepseek-ai/dsh-client-ui-session`
  （分别对应 locale / settingsScope / uiSession 三个服务提供者，保证先加载）。
- `peerDependencies` 跟进 0.1.2-rc.1（cordis ^4.0.2、dsh-client-locale/ui-settings/ui-session/settings ^0.1.2-rc.1）。
  提交给上游的 PR 版此处保持 `^0.1.1-rc.2` 不变（该范围本身已覆盖 0.1.2-rc.1 / 0.1.5-rc 系列）。

## 安装（本地 link，参照 profile 中 dsh-plugin-health 模式）

1. 编辑 `%DSH_HOME%\profiles\web\package.json`：
   - `dependencies` 增加：`"dsh-notice-center": "link:<项目目录>/dsh-notice-center"`
   - `dsh.profile.bundles` 数组增加：`"dsh-notice-center"`
   - 同步 `pnpm-lock.yaml` 的 link 条目（本机已手工补过；或执行 `pnpm install --offline`）
2. 插件自带 `cordis.patch.yml`（insert notice-center），随 bundle 层自动生效，**无需手改 profile 的 cordis.patch.yml**。
3. **重启要求分两半**：浏览器半 `lib/client.cjs` 改完**免重启**（`@deepseek-ai/dsh-client-hmr` 的宿主半会 stat-poll 各 client bundle，默认 500ms，检测到 mtime/size 变化即 `clientModuleHost.rebuilt(id)` 并经 `/plugins/events` SSE 通知浏览器重载；刷新页面可确保干净重载）；**宿主半 `lib/index.mjs` 改动必须重启 Harness**（宿主半热重载归 `cordis-plugin-hmr`，其构造要求 loader 带 `--expose-internals`，2026-09-13 实测未生效）。首次安装与任何改名同样必须重启。

## 验证

> **实测记录（2026-09-12，Harness 0.1.5-rc.1，客户端包 0.1.5-rc.2，即 PR 版代码）**：
> 琥珀 —— 审批请求弹出期间标签鲸鱼变琥珀、处理后熄灭 ✅（验证可选通道 `uiSession.pendingInteractions`）；
> 绿灯 —— 未选中跑完变绿（`completed`）与选中但标签不可见时完成变绿（`finishedWhileHidden`）两条路径均通过 ✅；
> 设置页三色读写正常 ✅。
> 注：本机 9/6 诊断时是 0.1.2-rc.1（插件适配点由该版本的类型定义推导），9/12 升级到 0.1.5-rc.1 后完成上述实测。

1. DevTools → Network，确认 `/plugins/dsh-done-whale/client.js`（或组合 bundle）为 200；Console 无 dsh-done-whale 相关报错。
2. **确认加载的是本版代码**：在 client bundle 响应里搜字符串 **`可选通道`**（旧单版本实现没有该注释；单版本实现把 `"uiSession"` 直接写在 `inject` 数组里）。
3. 绿灯：开一个会话 → 切到别的标签页/后台等它跑完 → 本页标签图标应变绿；点开该会话后恢复黑色。
4. 琥珀：触发向您提问/审批类交互且不处理 → 标签图标应变琥珀；处理后自动熄灭（本版改走官方 `uiSession.pendingInteractions`）。
5. 设置面板：设置 → 应出现「鲸鱼状态灯」分区（完成色/待处理色/默认色），改动即时生效。

## 卸载 / 回退

- 回退到同步前的单版本实现：把 `lib/*.bak-single-20260912-234344` 复制回对应文件名即可（HMR 会自动重载）。
- 卸载插件：移除 `package.json` 的 dependencies 条目与 `dsh.profile.bundles` 条目，刷新链接后重启即无残留；若插件管理器记了账，用 `dsh plugin --profile web remove dsh-done-whale` 更省事。
- 恢复原版：直接改用上游包 `dsh plugin --profile web add dsh-done-whale`（registry 版本仍是 v0.2.2，琥珀功能在当前版本不可用，见调研报告）。

## 文件清单

```
package.json                        适配后的包元数据（v0.2.2-adapt.1）
cordis.patch.yml                    与上游一致
lib/index.mjs                       宿主半（命名空间兼容写法 = PR 版）
lib/client.cjs                      浏览器半（可选通道 pending 写法 = PR 版）
lib/client.cjs.bak-single-*         同步前的单版本实现（回退用）
lib/index.mjs.bak-single-*          同步前的单版本实现（回退用）
LICENSE                             上游 MIT（未改）
适配说明.md                         本文件
```
