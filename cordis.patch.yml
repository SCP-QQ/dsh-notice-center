# dsh-notice-center bundle patch layer.
#
# 插入一行宿主插件：Loader 解析到 dsh-notice-center（profile node_modules），
# 运行宿主半（最小 cordis 插件）；client-modules 同时扫到本包的 dsh.client
# 元数据，把浏览器半挂进 /plugins/dsh-notice-center/client.js。
#
- insert:
    - id: notice-center
      name: 'dsh-notice-center'
