通知中心：「系统通知」也加折叠展开，子项默认收起

1. 两个分组条目统一由 sectionRow 构造：主标题 + 描述 + 官方 Switch + 折叠箭头，消除重复代码。
2. 「系统通知」新增独立展开态（notifyExpanded，默认折叠）：「仅当前页面不在前台时开启通知」改为由展开态控制显示（不再依赖通知开关是否打开），与颜色项的交互保持一致。
3. 新增 notifyExpand 文案（中英：更多通知设置 / More notification options），作为该展开按钮的 title 与 aria-label。
4. 验证：node 语法校验 + mock 冒烟测试（单一设置分区、uiSession 可选通道、完成与待处理两条通知路径）全部通过。
