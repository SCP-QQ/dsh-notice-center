通知中心：声音提醒改名为提示音，提示音行去掉副标题描述

1. 文案：notifySound「声音提醒」→「提示音」（英文 Sound alert → Sound）。
2. 布局：「完成提示音」「待处理提示音」两行去掉副标题描述（不再显示"默认（上行双音）/（下行双音）"），只保留标题与右侧试听按钮；soundRow 相应简化为 (kind, label) 两参。
3. 清理不再使用的 soundDoneDefault / soundPendingDefault 文案（中英各一条）。
4. 验证：node 语法校验 + mock 冒烟测试（完成与待处理两条通知路径、提示音与音量断言、单一设置分区）全部通过。
