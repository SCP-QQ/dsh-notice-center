通知中心：两个配置项统一为「主标题 + 描述 + 开关」结构

1. 「系统通知」条目改用主标题（原「开启系统通知」文案移除）：标题「系统通知」+ 描述「会话完成及有交互等待处理时发送通知」+ 右侧开关，与「鲸鱼状态灯」条目结构完全一致。
2. 移除最后一处 12px 分组标题及其样式常量（groupTitleStyle 不再使用），两组视觉语言统一；未再使用的 notifyEnabled 文案同步删除（中英各一条）。
3. 子项缩进保持一致：颜色项与「仅当前页面不在前台时开启通知」均为 16px 缩进，权限提示同样缩进。
4. 验证：node 语法校验 + mock 冒烟测试（单一设置分区、uiSession 可选通道、完成与待处理两条通知路径）全部通过。
