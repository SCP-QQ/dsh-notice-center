通知中心设置页：加分组标题，颜色项加 ↺ 恢复默认

1. 两个分组标题：「鲸鱼状态灯」与「系统通知」，样式对齐官方设置页的次级标题（12px / 500 / label-secondary，取自 settings-models 的 modelCatalogTitle）。
2. 「鲸鱼状态灯」组内的条目去掉重复标题，只保留描述「标签页图标根据任务状态显示不同颜色」+ 右侧总开关（switchRow 支持省略标题，Switch 的无障碍标签回退到描述）。
3. 颜色行（完成 / 待处理 / 默认色）在色板右侧新增 ↺ 按钮，点击调用 scope.unset(field) 恢复默认：green/amber 回官方色板，black 回「未配置 = 官方原版图标」；新增 restore 文案（中英各一条）。
4. 验证：node 语法校验 + mock 冒烟测试（单一设置分区、uiSession 可选通道、完成与待处理两条通知路径）全部通过。
