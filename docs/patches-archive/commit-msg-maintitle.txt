通知中心：「鲸鱼状态灯」改为配置项主标题（标题 + 描述两行结构）

1. 「鲸鱼状态灯」不再作为独立的 12px 分组标题，而是该配置项的主标题（14px / label-primary）；描述「标签页图标根据任务状态显示不同颜色」在其下方，右侧为总开关与展开箭头 —— 与官方设置项一致的 title + desc 结构。
2. 颜色项（完成 / 待处理 / 默认色）仍缩进 16px 作为其子项。
3. 「系统通知」分组标题保持现状（12px 次级标题），其下条目仍为「开启系统通知」+ 描述和缩进子项。
4. 验证：node 语法校验 + mock 冒烟测试（单一设置分区、uiSession 可选通道、完成与待处理两条通知路径）全部通过。
