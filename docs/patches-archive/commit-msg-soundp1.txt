通知中心 P1：音量滑杆 + 提示音回显 + 试听

1. 宿主 schema 新增 notifyVolume（0–1，默认 0.6）；设置页「系统通知」展开后新增三行：音量滑杆（原生 input[type=range] + accent-color 跟随主题，右侧显示百分比）、完成提示音、待处理提示音。
2. 提示音回显：两行分别显示当前音效描述（完成「默认（上行双音）」/ 待处理「默认（下行双音）」），为 P2 的自定义音效预留同一位置。
3. 试听：每行右侧播放按钮，按当前音量即时播放对应提示音（点击手势内触发，不受自动播放策略限制）。
4. 音量统一生效：playChime 增加 volume 参数，峰值增益 = 0.12 × volume；normalizeVolume 把配置收敛到 0–1（为 0 时直接静默）。
5. 验证：node 语法校验 + mock 冒烟测试（gains 断言为 [0.06,0.06,0.06,0.06] 即 0.12×0.5、chimes 为完成上行音与待处理下行音、两条通知路径、单一设置分区）全部通过。
