通知中心：颜色配置的鲸鱼图标去掉边框

1. 颜色子项的鲸鱼图标容器去掉 1px 描边与圆角，尺寸由 40×32 收缩为 28×28（仅保留图标本身的点击热区）；鲸鱼图标仍按当前配置色填充，点击仍由覆盖其上的隐藏 input[type=color] 调起取色器，写入逻辑不变。
2. 与右侧 ↺ 按钮的 28×28 尺寸对齐，行内右组视觉更整齐。
3. 验证：node 语法校验 + mock 冒烟测试（完成与待处理两条通知路径、通知 icon 为 data URI、单一设置分区）全部通过。
