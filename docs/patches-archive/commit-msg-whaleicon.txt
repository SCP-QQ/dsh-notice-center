通知中心：颜色预览改用鲸鱼图标，通知弹窗图标跟随配置色

1. 颜色子项（完成 / 待处理 / 默认色）的色板改为鲸鱼图标预览：用当前配置色填充（currentColor + 复用从 whaleSvg 提取的 path），外层仍是 40×32 的边框容器；点击容器任意处由覆盖其上的隐藏 input[type=color] 调起取色器，写入逻辑不变（scope.set）。
2. 系统通知的弹窗图标由官方 /favicon.svg 改为鲸鱼 data URI，颜色跟随配置：完成类通知用「完成色」，待处理类通知用「待处理色」（复用已有的 uri(hex) 生成）。
3. 新增模块级常量 WHALE_SHAPE_PATH（从 whaleSvg 提取 path 数据），供设置页图标预览与后续自定义渲染复用。
4. 验证：node 语法校验 + mock 冒烟测试（通知 icon 已断言为 data URI、完成与待处理两条触发路径、单一设置分区）全部通过。
